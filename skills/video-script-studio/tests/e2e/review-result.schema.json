{
  "type": "object",
  "additionalProperties": false,
  "required": ["project_path", "stage", "authored_artifacts"],
  "properties": {
    "project_path": {"type": "string"},
    "stage": {"enum": ["independent-review"]},
    "authored_artifacts": {
      "type": "object",
      "additionalProperties": false,
      "required": ["storyboard", "assets", "publish"],
      "properties": {
        "storyboard": {"enum": ["storyboard.md"]},
        "assets": {"enum": ["assets.md"]},
        "publish": {"enum": ["publish.md"]}
      }
    }
  }
}
