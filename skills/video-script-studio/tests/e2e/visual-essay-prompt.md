使用 $video-script-studio 创建一个新的、可恢复的视频脚本项目。所有信息都已给全，不需要猜测，也不要一次跨越多个确认门。

- 项目根目录：`__PROJECT_ROOT__`
- 标题：双重轨迹：骑行与版画
- 主类型：visual-essay
- 辅类型：创作者纪录片
- 平台与规格：YouTube，16:9，目标 90 秒
- 受众：喜欢动手创作、但害怕失败痕迹的年轻创作者
- 目标：让观众看到一次真实试做、失败与恢复，并愿意保留作品中的不完美
- 原创命题：把骑行轨迹与版画两项个人兴趣合成一件作品；不得复制 Gawx 的措辞、标题、镜头或具体作品
- 创作者档案（仅本项目使用，不持久更新）：少量第一人称旁白；优先可见行动、触感细节、环境声和静默；不做夸张结果承诺
- 制作资源：一名创作者、自有自行车、轮胎、版画纸、油墨、滚筒、刻刀、相机、手机录音；可拍室内工作台与户外骑行；无危险动作；低预算
- 权利：人物、地点和全部实拍素材均由创作者拥有并授权本项目使用；音乐只规划，不自动生成或声称已授权
- 事实边界：这是个人原创实验，无外部事实主张、无主动引用，不需要联网调研；必须在 research.md、sources.md 和项目状态中明确记录该决定
- 内容硬约束：至少五个场景并使用稳定编号 S01—S05；必须有轮胎纹理拓印的可见试做、油墨糊掉路径并撕裂纸面的失败、裂痕从失败痕迹变成路线的视觉母题变化、车轮空转/滚墨/撕纸等环境声；多数场景标记“无旁白”，旁白只用于不可见的意义转变
- 概念要求：后续概念阶段必须给出 A/B/C 三个实质不同方案；最终可选择“方案 B：双重轨迹”，但选择必须等 concepts.md 展示后由下一轮明确批准
- 时长方法：视觉随笔按五个场景 18、22、20、15、15 秒估算，总计 90 秒；后续必须实际运行确定性时长估算器并在脚本中记录结果
- 发布包装：需要 assets.md 与 publish.md，但不得自动生成媒体或发布

初始化时 `--platform` 必须逐字使用 `YouTube 16:9`。本轮只执行到 brief 确认门：初始化项目，只创建并展示 brief.md 的完整阶段内容；这里的“创建”只允许改写初始化器的 `brief.md` 骨架，不得改写初始化器已经创建的其他 Markdown 骨架，不得批准 brief，也不得提前创建或批准 research、concept、outline、script。`brief.md` 必须恰好使用以下五个二级标题并写入具体内容：`## 创作命题`、`## 受众与目标`、`## 制作边界`、`## 事实与权利边界`、`## 视觉随笔硬约束`；正文必须逐字保留 `visual-essay`、`骑行`、`版画`、`90`、`无外部事实` 和 `权利`。最终严格输出 JSON：`project_path` 为真实绝对路径，`awaiting_gate` 为 `brief`，`artifact` 为 `brief.md`。
